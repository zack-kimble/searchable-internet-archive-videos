name: searchable_public_meetings
channels:
  - defaults
dependencies:
  - nvidia:cudnn
  - pip
  - pip:
     - ruamel.yaml
     - internetarchive
     - python-ffmpeg
     - pytest
     - faster-whisper
     - tabulate
     - more-itertools
